const API=localStorage.getItem('restaurant_api_url')||'http://localhost:5000/api';let restaurants=[];const $=s=>document.querySelector(s);const money=n=>`USD ${Number(n||0).toFixed(2)}`;function toast(m){const t=$('#toast');t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}async function request(path,opt={}){const r=await fetch(API+path,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||'Request failed');return data}function renderStats(){const revenue=restaurants.reduce((a,r)=>a+Number(r.revenue||0),0),owed=restaurants.reduce((a,r)=>a+Number(r.owed||0),0),orders=restaurants.reduce((a,r)=>a+Number(r.orders||0),0);$('#stats').innerHTML=`<div class="stat"><span>Restaurants</span><strong>${restaurants.length}</strong></div><div class="stat"><span>Total Orders</span><strong>${orders}</strong></div><div class="stat"><span>Completed Revenue</span><strong>${money(revenue)}</strong></div><div class="stat"><span>Amount Owed to Me</span><strong>${money(owed)}</strong></div>`}function render(){const q=$('#search').value.toLowerCase();const list=restaurants.filter(r=>`${r.name} ${r.slug} ${r.status}`.toLowerCase().includes(q));$('#rows').innerHTML=list.map(r=>`<tr data-id="${r._id}"><td><div class="name">${esc(r.name)}</div></td><td><span class="slug">${esc(r.slug)}</span></td><td><span class="status ${r.status}">${esc(r.status)}</span></td><td>${r.orders||0}</td><td><div class="cell"><b>${Number(r.commission||0).toFixed(2)}%</b><button class="edit" data-edit="commission">Edit</button></div></td><td><div class="cell"><b>${esc(r.plan)}</b><button class="edit" data-edit="plan">Edit</button></div></td><td class="money">${money(r.revenue)}</td><td class="money">${money(r.owed)}</td><td><div class="cell"><span>${esc(r.whatsapp||'Not set')}</span><button class="edit" data-edit="whatsapp">Edit</button></div></td><td><div class="cell"><span>${esc(r.mobileMoney||'Not set')}</span><button class="edit" data-edit="mobileMoney">Edit</button></div></td><td><div class="cell"><button class="edit" data-action="toggle">${r.status==='active'?'Suspend':'Activate'}</button><button class="danger" data-action="delete">Delete</button></div></td></tr>`).join('')||'<tr><td colspan="11">No restaurants found.</td></tr>';renderStats()}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}async function load(){try{restaurants=await request('/restaurants');$('#apiStatus').textContent='API connected';$('#apiStatus').classList.add('ok')}catch(e){restaurants=[];$('#apiStatus').textContent='API offline';toast('Start the backend on port 5000.')}render()}function openForm(){ $('#restaurantForm').reset();$('#modalTitle').textContent='Create Restaurant';$('#modal').classList.remove('hidden')}$('#createBtn').onclick=openForm;document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>$('#modal').classList.add('hidden'));$('#search').oninput=render;$('#restaurantForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);const body=Object.fromEntries(f.entries());body.commission=Number(body.commission);try{await request('/restaurants',{method:'POST',body:JSON.stringify(body)});$('#modal').classList.add('hidden');toast('Restaurant created');await load()}catch(err){toast(err.message)}};$('#rows').onclick=async e=>{const b=e.target.closest('button');if(!b)return;const r=restaurants.find(x=>x._id===b.closest('tr').dataset.id);if(!r)return;if(b.dataset.action==='delete'){if(!confirm(`Delete ${r.name}?`))return;try{await request('/restaurants/'+r._id,{method:'DELETE'});toast('Restaurant deleted');await load()}catch(err){toast(err.message)}}else if(b.dataset.action==='toggle'){try{await request('/restaurants/'+r._id,{method:'PATCH',body:JSON.stringify({status:r.status==='active'?'suspended':'active'})});toast('Status updated');await load()}catch(err){toast(err.message)}}else if(b.dataset.edit){let value=prompt(`New ${b.dataset.edit}`,r[b.dataset.edit]??'');if(value===null)return;if(b.dataset.edit==='commission')value=Number(value);try{await request('/restaurants/'+r._id,{method:'PATCH',body:JSON.stringify({[b.dataset.edit]:value})});toast('Updated');await load()}catch(err){toast(err.message)}}};load();